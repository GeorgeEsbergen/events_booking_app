import 'package:flutter/material.dart';

import 'my_profile_body.dart';

class MyPorfile extends StatelessWidget {
  const MyPorfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: MyProfileBody(),
    );
  }
}